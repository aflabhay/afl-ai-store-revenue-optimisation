# Core solver
pulp>=2.7.0
highspy>=1.5.0        # HiGHS solver — arm64-native (Mac M1/M2); fallback to CBC on x86_64

# Data processing
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
scipy>=1.11.0         # KDE price break detection (gaussian_kde + argrelmin)
matplotlib>=3.7.0     # KDE plot generation (saved to data/processed/kde_plots/)

# Streamlit app
streamlit>=1.32.0
plotly>=5.18.0

# Database (Fabric connection)
pyodbc>=5.0.0
azure-identity>=1.15.0

# Notebooks
jupyter>=1.0.0

# Testing
pytest>=7.4.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
